# 群聊助手后续开发路线图

> 本文档用于记录后续长期开发任务。当前项目已经可以进行基础使用，本文中的未完成事项不代表现有功能不可运行。

## 状态说明

- `[ ]` 未开始
- `[-]` 进行中
- `[x]` 已完成
- `[!]` 受限、存在平台约束或需要进一步评估

## 当前可用基线

当前版本已经具备一条可运行的 QQ 群聊信息处理链路：

- QQ / OneBot 11 / NapCat 接入及多 QQ 账号连接
- 按平台、账号、群组区分和管理数据
- 群消息实时采集、保存、查询和筛选
- 授权时间记录、手动历史回填及按时间范围回填
- “不处理 / 仅记录消息 / 自动总结 / 总结并推送”处理模式
- 平台、账号、群组三级策略应用与优先级配置
- 手动总结、滚动时间窗总结和定时总结
- CodeBuddy、OpenAI-compatible、Ollama 总结 Provider
- 站内通知、浏览器前台通知和 SMTP 邮件
- 优先级规则、即时提醒、冷却和基础限流

当前定位是“可简单使用的个人自托管版本”。继续扩展前，应优先稳定内核，避免在单适配器和内存任务机制上直接堆叠多个平台。

## 近期任务：稳定现有功能

### 安全与访问控制

- [ ] 为管理页面、REST API 和 SSE 增加单管理员登录保护
- [ ] 首次启动生成管理员凭据，并提供安全轮换方式
- [ ] 避免 AI、平台和 SMTP 凭据出现在日志及接口响应中
- [ ] 为外部 Webhook、WebSocket 和机器人连接增加签名或 Token 校验
- [ ] 增加 Markdown XSS、命令参数注入和未授权访问测试

### 持久任务与故障恢复

- [ ] 建立 SQLite 持久任务队列，统一管理同步、回填、总结、推送和清理任务
- [ ] 支持任务超时、取消、重启恢复、指数退避和失败重试
- [ ] 增加死信任务和手动重试入口
- [ ] 保证总结与推送幂等，避免重启或重试后重复生成、重复发送
- [ ] 增加任务中心，展示等待、执行中、成功、失败和取消状态

### 测试与运维

- [ ] 引入单元测试、接口集成测试和关键流程端到端测试
- [ ] 覆盖授权边界、多账号隔离、消息去重、处理模式依赖和历史回填
- [ ] 覆盖总结时间窗、定时调度、通知限流、推送幂等和数据库迁移
- [ ] 增加结构化日志、运行健康状态和 Adapter / Provider / 推送通道诊断
- [ ] 增加数据库版本迁移、启动前备份、手动导出和恢复流程

## 中期任务：多平台群组能力

### 多适配器运行内核

- [ ] 将当前单 Adapter 实例改为可同时运行多个 Adapter 的管理器
- [ ] 所有动作按 `connectorId + accountId + groupId` 精确路由
- [ ] 为 Adapter 声明实时消息、历史消息、发送消息、成员和附件等能力
- [ ] 平台不支持某项能力时，在界面隐藏或禁用对应操作并说明原因
- [ ] 保证不同平台出现相同账号、群号或消息 ID 时不会发生数据冲突
- [ ] 增加各平台独立重连、限流、健康状态和错误隔离

### 第一批新增平台

- [ ] Telegram：机器人连接、群同步、实时消息、权限检测和发送消息
- [ ] Discord：Gateway、频道映射、必要 Intent、历史分页、线程元数据和发送消息
- [ ] 飞书：应用凭据、事件订阅验签、群同步、消息事件和卡片推送
- [!] 微信：暂不采用个人号非官方协议，优先评估企业微信等官方开放能力

### 平台无关消息模型

- [ ] 将 CQ 专用解析扩展为统一消息段模型
- [ ] 统一表示文本、提及、回复、引用、链接、图片、语音、视频和文件
- [ ] 支持平台消息编辑、删除和撤回事件
- [ ] 保存来源平台、账号、群组、原消息 ID 和可用的原消息链接
- [ ] 首版只保存附件元数据，用户点开时才按需加载临时资源
- [ ] 将 OCR、语音转写和文件内容摘要保留为远期可选能力

## 中期任务：本地 Agent 总结

- [ ] 建立统一 `SummaryProvider` 协议，包括探测、健康检查、默认模型、流式输出、取消和超时
- [ ] 保留现有 CodeBuddy SDK、OpenAI-compatible 和 Ollama Provider
- [ ] 接入本机已登录的 Codex，并默认使用 Agent 当前配置的模型
- [ ] 接入安装后的 Claude Code，并复用本地登录和默认模型
- [ ] 为其他本地 Agent 提供声明式命令适配方式
- [ ] 本地 Agent 通过非交互、临时会话和受限环境运行
- [ ] 总结任务不得获得文件修改、命令执行、MCP 或网络搜索权限
- [ ] 使用参数数组启动进程，不拼接 Shell 命令，限制运行时间和输出大小
- [ ] 本地 Agent 失败时只允许在本地 Provider 之间回退，不自动把群消息发送到云 API
- [ ] 云端 Provider 必须由用户或群策略显式授权
- [ ] Provider 与模型配置支持全局、平台、账号、群组逐级覆盖
- [ ] 保存 Provider、模型、提示词版本、消息数量、耗时和失败原因
- [ ] 增加总结结果评价、重新生成和不同模型结果对比

## 中期任务：Markdown 与信息聚合

### Markdown 完善

- [ ] Markdown 继续作为总结的标准存储格式
- [ ] 增加可版本化的总结模板和必需章节校验
- [ ] 缺少关键章节时自动修复格式，但不虚构聊天中不存在的信息
- [ ] 使用安全 Markdown 渲染器生成站内 HTML，过滤危险标签和链接
- [ ] 从同一份 Markdown 生成邮件 HTML、纯文本和平台卡片/消息
- [ ] 超出平台消息长度时按章节安全分段，并提供完整总结链接
- [ ] 支持复制 Markdown、复制纯文本、下载 `.md` 和打印/PDF
- [ ] 为结论、待办、风险和关键发言增加可回溯的原消息引用

### 跨群信息聚合

- [ ] 支持按平台、账号、自选群组集合和时间范围生成聚合总结
- [ ] 增加跨群日报、周报和专题报告
- [ ] 增加关键词全文检索、话题标签和重复信息合并
- [ ] 聚合内容必须保留来源群和原始消息引用
- [ ] 首版使用 SQLite 全文检索，确有需要后再评估向量检索
- [ ] 完成当前“提升总结权重”规则，使命中消息进入重点总结上下文

## 中期任务：多渠道可靠推送

### 推送模型

- [ ] 将总结生成、内容渲染、推送目标和投递任务完全解耦
- [ ] 来源群与推送目标独立配置，支持一个来源向多个目标推送
- [ ] 默认不向原群直接回复，只有显式配置后才允许发送
- [ ] 支持按平台、账号、群组、计划、通知类型和优先级配置路由规则
- [ ] 保存每次投递的等待、发送中、成功、失败和死信状态
- [ ] 增加投递幂等键、指数退避、失败重试和手动重发
- [ ] 按渠道执行 Markdown 渲染、长度裁剪、分段和频率限制
- [ ] 标记系统发送的总结消息，采集端忽略自身推送，防止消息回流循环

### 推送通道

- [x] 站内通知
- [x] SMTP 邮件基础能力
- [ ] 完善邮件 HTML 模板、收件人订阅和失败反馈
- [ ] QQ 机器人群消息，复用平台 Adapter 的发送能力
- [ ] Telegram、Discord 和飞书机器人目标
- [ ] 浏览器/PWA Web Push，支持页面关闭后的电脑和手机通知

## 中期任务：数据保留与清理

- [ ] 原始消息默认保留 90 天
- [ ] 保留期支持全局、平台、账号和群组逐级覆盖
- [ ] 总结和匿名统计默认长期保留
- [ ] 增加自动清理计划、预计数量与磁盘空间预览
- [ ] 增加按群彻底删除、账号解绑清理和附件缓存清理
- [ ] 所有清理操作记录操作者、时间、群组、时间范围和删除数量

### 清理已总结时间段的原始消息

- [ ] 用户选择群组和需要清理的起止时间
- [ ] 展示覆盖该范围的成功总结及预计删除的消息、附件数量
- [ ] 默认只删除至少被一个成功总结时间窗覆盖的原始消息
- [ ] 未被成功总结覆盖的消息默认保留
- [ ] 强制删除未覆盖内容必须使用独立选项和二次确认
- [ ] 清理完成后保留总结、聚合统计和不可逆操作审计
- [ ] 支持清理前创建数据库备份

## 远期任务

- [ ] 图片 OCR、语音转写、视频和文档内容抽取
- [ ] 跨平台用户身份关联，但默认不自动推断同一自然人
- [ ] 可选语义检索和本地向量模型
- [ ] 更细粒度的摘要模板、语言、长度和风格配置
- [ ] 移动端布局与完整 PWA 离线外壳
- [ ] 应用自身的容器化部署、升级检查和一键备份恢复
- [ ] 在确有共享需求后再评估多管理员、角色权限和审计审批

## 固定设计原则

1. 产品首先服务个人自托管，不提前引入多租户和计费复杂度。
2. 群组是消息采集、授权、总结、聚合和保留策略的基本单位。
3. 平台、账号和群组数据必须全链路隔离，不依据外部群号作为全局主键。
4. 先稳定内核，再接 Telegram、Discord 和飞书；微信优先采用官方能力。
5. Markdown 是标准总结格式，其他格式由渠道渲染器生成。
6. 优先复用本机 Agent 的登录状态和默认模型，禁止失败后自动上云。
7. 多媒体资源默认只保存元数据并按需加载。
8. 推送来源与目标解耦，默认不打扰原群。
9. 清理操作必须可预览、有审计，并优先保护未总结的原始信息。
10. 每个新增平台、Provider 和推送通道都必须通过统一合约测试。

